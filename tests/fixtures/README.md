# FTPS Integration Fixtures

The certificate and private key in this directory are short-lived, self-signed test fixtures used only by `ftps_integration_test`. They are not production credentials, are not trusted by the system CA store, and must never be reused by a deployed FTPS service.
